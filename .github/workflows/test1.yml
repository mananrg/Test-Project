name: test project
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Lint
        run: eslint --ext .jsx --fix src
      - name: Test
        run: vitest run
      - name: Build
        run: vite build
